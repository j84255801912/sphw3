#define BUFFER_SIZE (1000)
#define STRING_SIZE (1000)
#define NUM_DOCUMENTS (10)

#define ORDINARY_TIME (1000000)
#define URGENT_TIME (500000)
#define VERY_URGENT_TIME (200000)

enum {
    ORDINARY = 0,
    URGENT,
    VERY_URGENT
};

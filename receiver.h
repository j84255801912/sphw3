#define BUFFER_SIZE (1000)
#define STRING_SIZE (1000)
#define NUM_DOCUMENTS (10000)

#define ORDINARY_TIME (1.0)
#define URGENT_TIME (0.5)
#define VERY_URGENT_TIME (0.2)

enum {
    ORDINARY = 0,
    URGENT,
    VERY_URGENT
};
